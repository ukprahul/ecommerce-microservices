# Copy this to .env.local for local development
# Vite proxy handles these automatically in dev — only needed for production builds

VITE_PRODUCT_API_URL=http://localhost:8283
VITE_USER_API_URL=http://localhost:8282
